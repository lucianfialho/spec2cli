{
  "name": "mcp-c",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-c": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run && node bench/benchmark.mjs",
    "test:unit": "vitest run",
    "test:bench": "node bench/benchmark.mjs"
  },
  "keywords": ["mcp", "cli", "openapi", "ai-agents", "context-efficient"],
  "author": "",
  "license": "MIT",
  "description": "Context-efficient CLI protocol for AI agents. Progressive discovery + output envelope.",
  "dependencies": {
    "commander": "^13.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tiktoken": "^1.0.22",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
